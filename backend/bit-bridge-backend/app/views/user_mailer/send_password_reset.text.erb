BitBridge Global Password Reset

Hello <%= @user&.user_profile&.first_name.presence || @user&.email %>,

We received a request to reset your BitBridge Global account password.

Reset link (expires in <%= distance_of_time_in_words(@reset_password_within) %>):
<%= @url %>

If you did not request this password reset, ignore this email. Your password will remain unchanged.

Security notice:
BitBridge Global will never ask for your password, PIN, or OTP by email.

Support:
support@bitbridgeglobal.com
https://www.bitbridgeglobal.com
